var containsNearbyAlmostDuplicate = function(nums, indexDiff, valueDiff) {
    let left = 0;
    let right = nums.length;
    while(left<right){
        if(Math.abs(nums[left]-nums[right])<=indexDiff &&)
    }
};

let nums = [1,2,3,1]
let indexDiff = 3
let valueDiff = 0

console.log(nums,indexDiff,valueDiff);